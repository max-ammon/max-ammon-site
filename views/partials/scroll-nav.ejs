<%# The header's size follows the scroll position rather than switching state a
    moment after you move — see /js/nav-shrink.js. Included from the nav partial,
    so every page gets it. %>
<script src="/js/nav-shrink.js"></script>
